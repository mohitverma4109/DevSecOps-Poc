# Sample environment file - copy to .env for local runs.
# server.js will refuse to start if JWT_SECRET is not set.
PORT=3000
NODE_ENV=development
JWT_SECRET=REPLACE_WITH_A_RANDOM_32_BYTE_VALUE
# Generate one with: openssl rand -hex 32
ALLOWED_ORIGINS=http://localhost:3000
